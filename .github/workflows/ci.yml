name: CI

on:
  push:
    branches:
      - qa

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: test pipeline
        run: echo "CI funcionando correctamente"